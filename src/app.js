const div = document.getElementById("page-wrapper");

document.addEventListener("DOMContentLoaded",() =>{
    setTimeout(() => {
        div.style.top = "10px";
        div.style.opacity = 1; 
    }, 100);
});